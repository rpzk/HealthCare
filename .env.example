# Copie este arquivo para .env e ajuste os valores conforme seu ambiente local
# mv .env.example .env

# Banco de dados
POSTGRES_PASSWORD=troque_esta_senha

# NextAuth
# Use uma chave aleatória longa (>= 32 chars). Você pode gerar com: openssl rand -base64 48
NEXTAUTH_SECRET=uma_chave_secreta_bem_grande_e_aleatoria
# Aponte para o IP/local do seu mini PC (Umbrel) e porta 3000
NEXTAUTH_URL=http://192.168.0.100:3000

# Ollama IA Local
OLLAMA_URL=http://ollama:11434  # URL do serviço Ollama (já configurado no docker-compose)
OLLAMA_MODEL=llama3  # Modelo a ser usado (llama3, mistral, gemma, etc.)

# Outras (opcional)
# REDIS_HOST=redis  # já definido no docker-compose.yml para uso local
# NODE_ENV=development
# Variáveis de Ambiente - Renomeie para .env.local e configure os valores

# Database
DATABASE_URL="postgresql://username:password@localhost:5432/healthcare_db"

# NextAuth
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# Configuração do Google AI Studio
# IMPORTANTE: NUNCA COMMIT A CHAVE REAL!
# 1. Vá para https://makersuite.google.com/app/apikey
# 2. Crie uma nova API key
# 3. Copie a chave e substitua o valor abaixo
# 4. Este arquivo é apenas um exemplo - use .env.local para a chave real

GOOGLE_AI_API_KEY="sua_chave_da_api_do_google_ai_studio_aqui"

# App Configuration
APP_NAME="Sistema de Prontuário Eletrônico"
APP_VERSION="1.0.0"

# ==========================================
# CONFIGURAÇÃO DE EMAIL (para questionários)
# ==========================================
# Ative o envio de emails
EMAIL_ENABLED=true

# Email de origem (remetente)
EMAIL_FROM="HealthCare <noreply@suaclinica.com.br>"

# Provedor de email: "console" (apenas log), "smtp" (envia real)
EMAIL_PROVIDER=smtp

# Configurações SMTP (Gmail, Outlook, SendGrid, etc)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu_email@gmail.com
SMTP_PASS=sua_senha_de_app

# Para Gmail: Use "Senhas de app" em https://myaccount.google.com/apppasswords
# Para SendGrid: Use API Key como senha
# Para Outlook: smtp.office365.com porta 587

# ==========================================
# BACKUP EM GOOGLE DRIVE (OPCIONAL)
# ==========================================
# Para habilitar backup automatizado em Google Drive, configure:
# 1. Criar service account: https://console.cloud.google.com/iam-admin/serviceaccounts
# 2. Download JSON key e salve em: scripts/google-drive-key.json
# 3. Criar pasta em Google Drive para backups
# 4. Compartilhe a pasta com o email do service account
# 5. Configure as variáveis abaixo (ou deixe em branco para desativar)

# Conteúdo do JSON da Service Account (cole aqui, sem quebras de linha)
# Pode usar: cat scripts/google-drive-key.json | jq -c . para comprimir
GDRIVE_SERVICE_ACCOUNT_JSON=""

# ID da pasta no Google Drive para onde enviar os backups
# Encontre em: drive.google.com > abra a pasta > veja na URL: /folders/{FOLDER_ID}
GDRIVE_FOLDER_ID=""

# ==========================================
# OPENTELEMETRY TRACING CONFIGURATION
# ==========================================
# Enable distributed tracing with OpenTelemetry
OTEL_ENABLED=false
OTEL_SERVICE_NAME=healthcare-app
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces
# Set to '1' to disable tracing completely
# OTEL_TRACING_DISABLED=1
