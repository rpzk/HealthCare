# Copie este arquivo para .env e ajuste os valores conforme seu ambiente local
# mv .env.example .env

# Banco de dados
POSTGRES_PASSWORD=troque_esta_senha

# NextAuth
# Use uma chave aleatória longa (>= 32 chars). Você pode gerar com: openssl rand -base64 48
NEXTAUTH_SECRET=uma_chave_secreta_bem_grande_e_aleatoria
# Aponte para o IP/local do seu mini PC (Umbrel) e porta 3000
NEXTAUTH_URL=http://192.168.0.100:3000

# Ollama IA Local
OLLAMA_URL=http://ollama:11434  # URL do serviço Ollama (já configurado no docker-compose)
OLLAMA_MODEL=llama3  # Modelo a ser usado (llama3, mistral, gemma, etc.)

# Outras (opcional)
# REDIS_HOST=redis  # já definido no docker-compose.yml para uso local
# NODE_ENV=development
# Variáveis de Ambiente - Renomeie para .env.local e configure os valores

# Database
DATABASE_URL="postgresql://username:password@localhost:5432/healthcare_db"

# NextAuth
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# Configuração do Google AI Studio
# IMPORTANTE: NUNCA COMMIT A CHAVE REAL!
# 1. Vá para https://makersuite.google.com/app/apikey
# 2. Crie uma nova API key
# 3. Copie a chave e substitua o valor abaixo
# 4. Este arquivo é apenas um exemplo - use .env.local para a chave real

GOOGLE_AI_API_KEY="sua_chave_da_api_do_google_ai_studio_aqui"

# App Configuration
APP_NAME="Sistema de Prontuário Eletrônico"
APP_VERSION="1.0.0"
